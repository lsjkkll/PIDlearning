# 位置式 PID 学习笔记

> 本文基于本人从零推导 + 亲手调试验证，非抄录。

------

## 一、一句话理解

PID 只盯一个数字：**误差 = 目标值 − 当前值**。

- 误差大 → 用力
- 误差小 → 轻手
- 方向反了 → 往另一头顶

它不看"你怎么操作"，只看"现在离目标还差多少"。

------

## 二、三个分量

### P（比例）

```
手劲 = Kp × 误差
```

- 误差为 0 → 手劲为 0
- 误差越大 → 手劲越大
- **是主力，决定反应有多猛**

### I（积分）

把**历史误差一路累加**，用来补"稳态误差"。

**为什么需要它：** 当存在外界消耗（散热、负载）时，纯 P 会卡在目标下方。那个卡住的位置，误差**不是 0**，而是一个让 P 的力恰好等于外界消耗的**固定小残差**——这就是**稳态误差**。

P 只认"现在差多少"，差 0 就没力，所以它天生看不见"需要一直出力才能维持"的需求。I 就是那个记住历史老账的力：哪怕现在误差是 0，只要总账还欠着，它就继续出力，把最后那口气补上。

### D（微分）

看误差的**变化率**（斜率 / 导数）：

```
Δ = 本次误差 − 上次误差
```

| 情况                             | D 的符号 | 与 P 的关系 | 作用               |
| :------------------------------- | :------- | :---------- | :----------------- |
| 误差快速**变小**（正在逼近目标） | 负       | 反向        | **刹车**，防冲过头 |
| 误差**变大**（正在远离目标）     | 正       | 同向        | 帮着**拉回**       |

**关键：** D 到底"刹车还是加油"，取决于误差此刻在变大还是变小，**不是一味刹车**。

> D 是减震器（阻尼），但**会放大噪声**，不是越大越好。实际工程里常配低通滤波。

------

## 三、闭环反馈（PID 之所以叫"回路"）

```
error = setpoint - current;    // 每拍重新算
output = kp*pid_p + ki*pid_i + kd*pid_d;
current += output * 系数;       // 输出反过来推动系统
```

每拍都用**最新的 current** 重新算误差，输出又反过来推动 current——形成闭环。

------

## 四、跨拍状态必须放循环外

以下变量必须声明在**循环外**，否则每拍被重建、历史丢失：

| 变量                 | 作用              | 放错的后果         |
| :------------------- | :---------------- | :----------------- |
| `temp`（上一拍误差） | 给 D 提供"上一次" | D 恒为 0           |
| `pid_i`（积分累计）  | 记住历史          | 积分清零，退化成 P |
| `count`（计数器）    | 判断是否首拍      | 分支永远走同一支   |

> 首拍的 D 天然为 0 —— 因为没有"上一拍"可比，这不是 bug。

------

## 五、积分饱和（integral windup）

**现象：** 误差长期同一方向不变 → 积分一直累加 → 输出被顶到天文数字、饱和，无法拉回。

**必须限幅：**

```
if (pid_i > MAX)      pid_i = MAX;
else if (pid_i < -MAX) pid_i = -MAX;
```

------

## 六、完整代码（本人手写并跑通）

```
#include <iostream>
using namespace std;

int main() {
    static float MAX = 100;
    int range = 100;
    double error;
    float kp = 0.5;
    float ki = 0.05;
    float kd = 0.1;
    double output;
    double pid_p = 0, pid_i = 0, pid_d = 0;   // 状态量放循环外
    double current = 8.0;
    double temp = 0;                           // 上一拍误差,循环外
    double setpoint = 100.0;
    int count = 0;                             // 计数器,循环外

    for (int i = 0; i <= range; i++) {
        error = setpoint - current;
        pid_p = error;
        pid_i += error;
        if (pid_i > MAX)       pid_i = MAX;     // 抗积分饱和
        else if (pid_i < -MAX) pid_i = -MAX;

        count++;
        if (count == 1) {
            temp = error;                       // 首拍无上一拍
        } else {
            pid_d = error - temp;
            temp = error;
        }

        output = ki * pid_i + kp * pid_p + kd * pid_d;
        cout << output << "\t" << current << endl;
        current += output * 0.5;                // 闭环反馈
    }
}
```

------

## 七、调参直觉（三者权衡）

| 系数   | 作用               | 调太大的后果   | 调太小的后果          |
| :----- | :----------------- | :------------- | :-------------------- |
| **Kp** | 反应猛不猛         | 超调、震荡     | 反应迟钝              |
| **Ki** | 最后那口气补得多快 | 超调、震荡     | 稳态误差补不掉 / 很慢 |
| **Kd** | 刹车 / 减震        | 放大噪声、抖动 | 冲过头、回摆久        |

------

## 八、观察到的现象

调 `kp=0.5, ki=0.05, kd=0.1` 跑 100 拍：current 从 8 一路爬升逼近 100，中途会**超调（冲过头）再回摆**，最后收敛。

> 超调的主因往往是**积分在持续累积**：即使越过了目标，`Ki*Σe` 那份历史总账仍是正值，把 P 的反向力盖住，继续把系统往前推。