# 增量式 PID 学习笔记

> 本文基于本人从零推导公式 + 亲手调试跑通，非抄录。

------

## 一、与位置式的根本区别

|          | 位置式                       | 增量式                              |
| :------- | :--------------------------- | :---------------------------------- |
| 输出     | **绝对力道**（一个具体数值） | **变化量 Δ**                        |
| 真正输出 | 直接就是 output              | `output_k = output_(k-1) + Δoutput` |

位置式喂"力道"，增量式喂"变化量"。

------

## 二、公式推导（由位置式相邻两拍相减而来）

位置式第 k 拍的完整项是 `P + I + D`。增量式要算 `Δoutput = Output_k − Output_(k-1)`，也就是三项**各自相减**：

### ΔP

```
ΔP = Kp*(e_k − e_(k-1))
```

### ΔI（关键：相减后只剩本拍）

```
ΔI = Ki*e_k
```

> 推导：`I_k − I_(k-1) = Ki * (前k拍总和 − 前k−1拍总和)`，中间项全部抵消，**只剩第 k 拍那一个 error**。

### ΔD

```
ΔD = Kd*(e_k − 2*e_(k-1) + e_(k-2))
```

> 推导：`D_k − D_(k-1) = Kd*(e_k − e_(k−1)) − Kd*(e_(k−1) − e_(k−2))`，展开合并后系数为 **1, −2, 1**。

### 合成

```
Δoutput = ΔP + ΔI + ΔD
output_k = output_(k-1) + Δoutput
```

------

## 三、需要三个误差变量（滑动窗口）

因为 D 用到 `e_k, e_(k-1), e_(k-2)`，所以要存**三拍**。更新顺序（旧值往更旧退，最后填新值）：

```
error_k1 = error_k2;             // 上上拍 <- 上一拍
error_k2 = error_k3;             // 上一拍 <- 本拍
error_k3 = setpoint - current;   // 本拍 <- 新误差
```

> **顺序写反**（例如写成 `error_k3 = error_k2;`）→ 窗口滑不动 → `e_k − e_(k-1) = 0` → **D 恒为 0**。

------

## 四、累加输出（最容易漏的一步）

```
output_k1 = output_k2;              // 上一拍输出 <- 本拍输出（保存）
output_k2 = output_k1 + d_output;   // 本拍输出 = 上一拍 + 增量
```

> **漏掉 `output_k1` 的更新** → 每拍都从 0 起步 → 输出不累加 → 收敛极慢（表现为 output 单调递减、current 爬得极慢）。

------

## 五、反直觉性质（P、D 只看"变化"，I 才看"水平"）

增量式里：

- `ΔP` 是误差的**差**，`ΔD` 也是误差的**差** → 误差恒定不变时，二者恒为 **0**
- `ΔI = Ki*e_k` 看的是误差的**绝对水平** → 唯一持续出力的

**推论（重要）：** 当"误差恒定 + 输出从 0 起步"时，**只有 I 能产生第一个非零增量**。

> 所以把 `ki` 设为 0，系统**第一拍 d_output 就是 0，从此动不了**——不是"有静差"，而是"根本启动不了"。

------

## 六、增量式仍需抗饱和限幅

增量式少了一个**显式**积分器，但**并不免疫饱和**： 执行器被物理限死时，计算侧那个 `output` 会**继续累加**（它不知道自己被削顶了），一旦环境恢复，要"卸掉"虚高的积累才能回来，这段时间系统失控。

> 结论：**"不容易饱和" ≠ "不需要饱和保护"**，增量式照样要限幅。

------

## 七、初始化敏感

上一拍误差、`output` 初值设错，会导致启动迟钝、甚至像第五节那样完全动不了。

------

## 八、适用场合

| 形式       | 适合的执行机构               | 原因                                                         |
| :--------- | :--------------------------- | :----------------------------------------------------------- |
| **增量式** | 步进电机等**离散**执行机构   | 执行器**自带积分**（转了 N 步 = 增量累加），"喂一个 Δ"直接对上物理语言 |
| **位置式** | 阀门等需要**绝对位置**的场合 | 执行器要的是"停在 60%"这个绝对量                             |

------

## 九、完整代码（本人手写并跑通）

```
#include <iostream>
using namespace std;

int main() {
    static float MAX = 100;
    int range = 100;

    double error_k1, error_k2, error_k3;   // e_(k-2), e_(k-1), e_k
    float kp = 0.5;
    float ki = 0.1;
    float kd = 0.45;

    double d_output = 0.0, output_k1 = 0.0, output_k2 = 0.0;
    double d_pid_p = 0, d_pid_i = 0, d_pid_d = 0;
    double current = 8.0;
    double setpoint = 100.0;

    error_k1 = error_k2 = error_k3 = setpoint - current;  // 初始化

    for (int i = 0; i <= range; i++) {
        // 1. 滑动窗口，旧值往更旧退
        error_k1 = error_k2;
        error_k2 = error_k3;
        error_k3 = setpoint - current;

        // 2. 三个增量项
        d_pid_p = kp * (error_k3 - error_k2);
        d_pid_i = ki * (error_k3);
        d_pid_d = kd * (error_k3 - 2 * error_k2 + error_k1);
        d_output = d_pid_d + d_pid_i + d_pid_p;

        // 3. 累加输出（关键，不能漏 output_k1 的更新）
        output_k1 = output_k2;
        output_k2 = output_k1 + d_output;

        // 4. 闭环反馈
        current += output_k2 * 0.5;
        cout << output_k2 << "\t|\t" << current << endl;
    }
}
```

**运行结果（kp=0.5, ki=0.1, kd=0.45）：**

```
current: 12.6 → 19.4 → 28 → 37.7 → …… → 112.1（超调）
        → 111.9 → 111.3 → …… → 100.2 → 98.4（回摆）
        → 99.6 → 100.1 → …… → 100.00（稳住）
```

------

## 十、调试实录（本人踩过的三个坑）

1. **窗口滑反** —— 写成 `error_k3 = error_k2;` → `error_k2` 从未被赋值 → D 恒为 0。 *诊断法：手算第一拍、第二拍三个变量的值，看谁从没变过。*
2. **`output_k1` 没更新** —— 每拍输出从 0 起步 → 不累加 → 收敛极慢。 *症状：output 单调递减到接近 0，而不是累加。*
3. **`ki = 0` 启动不了** —— 首拍 dP=dD=0，dI=0 → output=0 → current 永远卡在初值。 *教训：增量式里 P、D 只对"误差的变化"反应，只有 I 对"误差本身"反应。*

------

## 十一、调参提示（结合位置式笔记）

- 超调主因常见于 **I 累积**：越过目标后 `Ki*Σe` 仍是正值，压住 P 的反向力。
- 压超调可 **增大 Kd**（增强刹车）或 **减小 Kp**（降低初始推力）。
- 但 **减小 Ki 的代价**是"最后那口气补得更慢 / 补不掉"，要在超调与稳态误差之间权衡。